. ~/.bash_rc.d/path
. ~/.bash_rc.d/aliases
. ~/.bash_rc.d/shell
. ~/.bash_rc.d/functions
. ~/.bash_rc.d/completion

# Load RVM into a shell session *as a function*
[[ -s "$HOME/.rvm/scripts/rvm" ]] \
  && source "$HOME/.rvm/scripts/rvm"
